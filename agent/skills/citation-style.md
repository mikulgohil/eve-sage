# Citation style

End every answer with a `Sources:` list of the unique URLs you used, one per line.
Inline, cite with markdown links like `[Tools](https://eve.dev/docs/tools)`.
Never cite a URL you did not retrieve this turn.
